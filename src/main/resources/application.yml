spring:
  # ?????? ?? ??
  datasource:
    url: jdbc:postgresql://localhost:5432/lotto  # ?? DB ??
    username: lotto_id                              # ?? ??
    password: lotto_pw                              # ????
    driver-class-name: org.postgresql.Driver

  # JPA (Java Persistence API) ??
  jpa:
    hibernate:
      # ddl-auto: ?????? ?? ??? DB ???(??? ?)? ??? ?? ??
      #   - create: ?? ???? ???? ?? ?? (?? ?? ??? ??)
      #   - update: ??? ??? ??
      #   - validate: ???? ???? ?? ?????? ??
      #   - none: ???? ?? ?? (?? ???? ??)
      ddl-auto: create
    properties:
      hibernate:
        # ??? JPA? ???? SQL ??? ???
        show_sql: true
        # SQL ??? ?? ?? ???
        format_sql: true
        # ?? ??? ??? ?? (PostgreSQL?? user?? ??? ??? ??? ??? ??)
        default_schema: public
    # ?????? ??? ????? ??
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# ?? ?? ?? (JPA ?? ???? ?? ??? ???)
logging:
  level:
    org.hibernate.type.descriptor.sql: trace